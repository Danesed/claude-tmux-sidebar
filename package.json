{
  "name": "claude-tmux-sidebar",
  "displayName": "AgentMux — Claude & Codex in tmux",
  "description": "Two agents, one terminal flow: switch fast and hand off work intelligently between Claude Code and Codex in tmux.",
  "version": "0.9.0",
  "publisher": "Danesed",
  "license": "MIT",
  "icon": "icon.png",
  "engines": { "vscode": "^1.80.0" },
  "categories": ["Other", "AI", "Machine Learning"],
  "keywords": ["claude", "claude-code", "codex", "gpt", "tmux", "terminal", "sidebar", "ai", "remote-ssh", "agent"],
  "galleryBanner": { "color": "#0e1b2a", "theme": "dark" },
  "repository": { "type": "git", "url": "https://github.com/Danesed/claude-tmux-sidebar" },
  "bugs": { "url": "https://github.com/Danesed/claude-tmux-sidebar/issues" },
  "homepage": "https://github.com/Danesed/claude-tmux-sidebar#readme",
  "activationEvents": [],
  "main": "./extension.js",
  "scripts": {
    "check": "node --check extension.js && node --check media/main.js && node test/test.js",
    "test": "node test/test.js",
    "package": "npx @vscode/vsce package"
  },
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "claudeTmux",
          "title": "AgentMux",
          "icon": "media/icon.svg"
        }
      ]
    },
    "views": {
      "claudeTmux": [
        {
          "id": "claudeTmux.view",
          "name": "AgentMux",
          "type": "webview"
        }
      ]
    },
    "commands": [
      {
        "command": "claudeTmux.attach",
        "title": "Resume / switch active agent session…",
        "category": "AgentMux",
        "icon": "$(history)"
      },
      {
        "command": "claudeTmux.restart",
        "title": "Restart active agent in this session",
        "category": "AgentMux",
        "icon": "$(refresh)"
      },
      {
        "command": "claudeTmux.kill",
        "title": "Kill active agent session",
        "category": "AgentMux",
        "icon": "$(trash)"
      },
      {
        "command": "claudeTmux.killPick",
        "title": "Manage this workspace's tmux sessions…",
        "category": "AgentMux",
        "icon": "$(list-selection)"
      },
      {
        "command": "claudeTmux.handoff",
        "title": "Hand off to the other agent…",
        "category": "AgentMux",
        "icon": "$(arrow-swap)"
      }
    ],
    "menus": {
      "view/title": [
        { "command": "claudeTmux.attach", "when": "view == claudeTmux.view", "group": "navigation@1" },
        { "command": "claudeTmux.restart", "when": "view == claudeTmux.view", "group": "navigation@2" },
        { "command": "claudeTmux.handoff", "when": "view == claudeTmux.view", "group": "navigation@3" },
        { "command": "claudeTmux.kill", "when": "view == claudeTmux.view", "group": "navigation@4" },
        { "command": "claudeTmux.killPick", "when": "view == claudeTmux.view", "group": "1_manage@1" }
      ],
      "commandPalette": [
        { "command": "claudeTmux.attach" },
        { "command": "claudeTmux.restart" },
        { "command": "claudeTmux.handoff" },
        { "command": "claudeTmux.kill" },
        { "command": "claudeTmux.killPick" }
      ]
    },
    "keybindings": [
      {
        "command": "claudeTmux.view.focus",
        "key": "ctrl+shift+a",
        "mac": "cmd+shift+a"
      }
    ],
    "configuration": {
      "title": "AgentMux",
      "properties": {
        "claudeTmux.refreshMs": {
          "type": "number",
          "default": 120,
          "minimum": 80,
          "description": "How often (ms) the side-bar mirror refreshes the tmux pane."
        },
        "claudeTmux.claudeArgs": {
          "type": "string",
          "default": "--dangerously-skip-permissions --ide",
          "description": "Arguments passed to `claude` when starting or resuming a session."
        },
        "claudeTmux.codexArgs": {
          "type": "string",
          "default": "--no-alt-screen",
          "description": "Arguments passed to `codex` when starting or resuming a session. `--no-alt-screen` preserves terminal scrollback."
        },
        "claudeTmux.codexFullAccess": {
          "type": "boolean",
          "default": true,
          "description": "Start and resume Codex with Full Access (`--dangerously-bypass-approvals-and-sandbox`). This disables approval prompts and sandbox protection; turn it off when working in an untrusted repository."
        },
        "claudeTmux.codexReadClaudeRules": {
          "type": "boolean",
          "default": true,
          "description": "Use per-launch developer instructions to make Codex recursively read every Markdown file under workspace `.claude`. The files are not modified. An explicit developer_instructions value in codexArgs takes precedence."
        },
        "claudeTmux.sessionPrefix": {
          "type": "string",
          "default": "tmux_",
          "description": "tmux session name prefix. Session = <prefix><folder name> (e.g. tmux_my-project)."
        },
        "claudeTmux.codexSessionPrefix": {
          "type": "string",
          "default": "codex_",
          "description": "Codex tmux session prefix. Session = <prefix><folder name> (e.g. codex_my-project)."
        },
        "claudeTmux.scrollbackLines": {
          "type": "number",
          "default": 1000,
          "minimum": 0,
          "maximum": 5000,
          "description": "Maximum tmux history lines captured for scrolling in the side-bar mirror."
        },
        "claudeTmux.fontFamily": {
          "type": "string",
          "default": "",
          "description": "Font for the mirror. Empty = inherit terminal.integrated.fontFamily (so Nerd Font / Powerline glyphs match your real terminal)."
        },
        "claudeTmux.fontSize": {
          "type": "number",
          "default": 0,
          "description": "Font size for the mirror in px. 0 = inherit terminal.integrated.fontSize (fallback 12)."
        },
        "claudeTmux.cursorStyle": {
          "type": "string",
          "enum": ["block", "bar", "underline"],
          "default": "block",
          "description": "Cursor shape in the mirror."
        },
        "claudeTmux.autoResume": {
          "type": "boolean",
          "default": false,
          "description": "When the side bar opens and no Claude session is running, automatically resume the folder's most recent Claude conversation. Disabled by default so tabs only appear after an explicit start or an existing tmux session is detected."
        }
      }
    }
  }
}
